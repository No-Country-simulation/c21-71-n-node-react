{
  "name": "proyecto-adopcion",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "workspaces": [
    "./apps/*",
    "./packages/*"
  ],
  "scripts": {
    "build:server-app": "npm run build --prefix apps/server-app && npm run migrate --prefix apps/server-app ",
    "start:server-app": "npm run start --prefix apps/server-app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^22.7.4"
  }
}
